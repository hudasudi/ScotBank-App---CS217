<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Account Details</title>
        <link rel="stylesheet" href="../assets/static/css/account/summary.css">
    </head>
    <body>
        <div class="header">
            <h1>Account Spending</h1>
        </div>

        <nav class="navbar">
            <a href="/accounts/dashboard?uuid={{account.uuid}}">Home</a>
            <a href="/accounts/details?uuid={{account.uuid}}">Account Details</a>
            <a href="/accounts/transactions?uuid={{account.uuid}}">Transaction Details</a>
            <a class="active" href="/accounts/summary?uuid={{account.uuid}}">Account Summary</a>
        </nav>

        <section class="table-container">
            <div class="spending">
                <h2>Summary of Spending</h2>

                <table>
                    <thead>
                        <tr>
                            <th>Category</th>
                            <th>Date</th>
                            <th>Amount Spent (£)</th>
                        </tr>
                    </thead>

                    <tbody>
                        {{#each categories}}
                            <tr>
                                <td>{{this.key}}</td>
                                <td>??/??/????</td>
                                <td>{{this.value}}</td>
                            </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </section>
    </body>
</html>