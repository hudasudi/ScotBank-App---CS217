<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Account API</title>

        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

        <style>
            :root {
                --bg-clr: #111;
                --txt-clr: #fff;
            }

            body {
                background-color: var(--bg-clr);
                color: var(--txt-clr);
                font-size: 1rem;
            }

            h1.page-title {
                margin: 50px;
                margin-bottom: 20px;
                text-align: center;
                padding-top: 50px;
            }

            div.accounts-list {
                display: flex;
                justify-content: center;
                margin-top: 60px;
            }

            div.account-widget {
                background-color: #0E402D;
                padding: 20px;
                border-radius: 10px;
                margin: 20px;

                width: 50vw;
                height: 50vh;
            }

            div.account-widget h3.user-or-admin {
                text-align: center;
                margin-top: 20px;
            }

            div.account-widget div.acc-info p.account-bal,
            div.account-widget div.acc-info p.account-round,
            div.account-widget div.acc-info p.account-name,
            div.account-widget div.acc-info p.account-uuid {
                font-size: 2rem;
                margin-left: 20px;
                margin-top: 40px;
            }

            div.account-widget div.acc-info {
                padding: 10px;
                margin: 0;
            }
        </style>
    </head>
    <body>
        <h1 class="page-title">Account Information On: <b>{{name}}</b></h1>

        <div class="accounts-list">
            <div class="account-widget">
                <div class="acc-info">
                    <p class="account-name">Name: <b>{{name}}</b></p>
                    <p class="account-bal">Balance: £<b>{{bal}}</b></p>
                    <p class="account-round">Round Up? <b>{{round}}</b></p>
                </div>

                <h3 class="user-or-admin">YOU ARE A USER</h3>
            </div>
        </div>

        <script
                src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"
                integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6"
                crossorigin="anonymous">
        </script>
    </body>
</html>